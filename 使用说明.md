# Oracle 全球区域 TCP 延迟监控

这是无需安装 Python 的 Windows 单文件版。解压后只需双击 `OracleTCPMonitor.exe`，即可开始 48 小时监控。程序会自动打开：

`http://127.0.0.1:8765`

## 测试前先关闭梯子

如果你要测的是本机宽带或当前运营商直连 Oracle 的真实延迟，运行前建议关闭：

- VPN、WireGuard、OpenVPN 等隧道
- Clash、sing-box、v2rayN 等工具的 TUN 模式或全局代理
- 代理客户端创建的虚拟网卡
- 游戏加速器及其他可能修改系统路由的软件

否则测得的可能是“代理节点 → Oracle”的线路，而不是“你的电脑 → Oracle”的直连线路。仅在浏览器中配置的 HTTP 代理通常不会影响本程序的原始 TCP 连接，但为保证结果可比较，测试期间仍建议关闭所有代理工具。

## 功能

- 每 60 秒并行检测 Oracle 官方区域表中的 45 个 Object Storage TCP 443 端点
- 自动记录当前值、平均值、P95、抖动、成功率和失败原因
- 数据存入本目录的 `oracle_latency.db`，关闭后不会丢失
- 网页可切换 1 小时、6 小时、24 小时、48 小时、7 天和 30 天统计窗口
- 点击任一地区可查看历史趋势
- 点击“导出 CSV”保存原始检测记录
- “暂停”停止后续检测但保留已有数据；“继续”从暂停处恢复
- “重新开始”会在确认后清空全部历史、轮次归零，并重新计算 48 小时监控时长

## 启动方式

- 双击 `OracleTCPMonitor.exe`：运行满 48 小时后停止继续采集，网页保持可查看到窗口关闭。
- 需要不限时运行时，可在命令行先设置 `TCP_DURATION_HOURS=0` 再启动 EXE。

请不要直接在 ZIP 压缩包预览窗口中运行；先把 EXE 解压到普通文件夹。请不要同时启动两次，否则会重复写入检测数据并争用网页端口。

## 修改参数

高级用户可通过环境变量修改：

- `TCP_INTERVAL=60`：检测间隔，单位秒，最低 5 秒
- `TCP_TIMEOUT=5`：单个地址连接超时，单位秒
- `TCP_DURATION_HOURS=48`：监控时长；设为 0 表示不限时

区域清单已嵌入 EXE，默认包含 Oracle 官方区域表的 45 个端点。

## 注意

- 测量的是到 Oracle Object Storage 服务入口的 TCP 建连时间，适合比较区域线路，但不等同于某台未来 VPS 的完整业务延迟。
- DNS 不通、端口不通或连接超时会记录为失败，不会用超时秒数伪装成延迟。
- SQLite 使用 WAL 模式，48 小时约产生 6.5 万条记录，普通电脑可轻松保存。
