# Oracle 全球区域 TCP 延迟监控

## 应用介绍

Oracle 的中文名称是**甲骨文**。这个应用程序用于测试**你当前所在地的网络到甲骨文云（Oracle Cloud Infrastructure，简称 OCI）各区域服务器的连接速度**。

程序会同时连接各区域的 Object Storage HTTPS 服务端口（TCP 443），并统计当前延迟、平均延迟、P95、抖动和成功率。它可以帮助你比较本地宽带或当前运营商到不同甲骨文云区域的线路质量，从而挑选连接更快、更稳定的区域。

测量结果代表当前电脑到甲骨文云区域服务入口的 TCP 建连速度，适合比较区域线路，但不等同于某台具体云服务器的完整业务速度或下载带宽。

这是无需安装 Python 的 Windows 单文件版。解压后只需双击 `OracleTCPMonitor.exe`，即可开始 48 小时监控。程序会自动打开：

`http://127.0.0.1:8765`

## 测试前先关闭梯子

如果你要测的是本机宽带或当前运营商直连 Oracle 的真实延迟，运行前建议关闭：

- VPN、WireGuard、OpenVPN 等隧道
- Clash、sing-box、v2rayN 等工具的 TUN 模式或全局代理
- 代理客户端创建的虚拟网卡
- 游戏加速器及其他可能修改系统路由的软件

否则测得的可能是“代理节点 → Oracle”的线路，而不是“你的电脑 → Oracle”的直连线路。仅在浏览器中配置的 HTTP 代理通常不会影响本程序的原始 TCP 连接，但为保证结果可比较，测试期间仍建议关闭所有代理工具。

## 功能

- 每 60 秒并行检测 Oracle 官方区域表中的 45 个 Object Storage TCP 443 端点
- 自动记录当前值、平均值、P95、抖动、成功率和失败原因
- 数据存入本目录的 `oracle_latency.db`，关闭后不会丢失
- 网页可切换 1 小时、6 小时、24 小时、48 小时、7 天和 30 天统计窗口
- 点击任一地区可查看历史趋势
- 点击“导出 CSV”保存原始检测记录
- 页面顶部同时显示公网 IPv4、IPv6、中文运营商及 IP 数据库推测的位置；可填写自定义网络标注，CSV 导出也会附带这些信息
- 每次打开网页或切回页面标签时都会重新检测公网出口网络；切换 Wi-Fi、手机热点或有线网络后自动识别变化并刷新显示，不会继续显示上一次网络的信息
- 为完成查询，程序会将公网 IP 发送给 `ipwho.is`，失败时使用 `ipapi.co`；位置可能是运营商注册地，不代表精确住址
- 可在页面顶部新建、切换测试项目；每个项目独立显示排名、曲线并导出 CSV
- 到新地点测试时，先创建类似“家里联通”“公司 Wi-Fi”“酒店网络”的项目，再开始采集；原有数据自动保存在“默认项目”
- 每次打开程序会先暂停并弹出项目管理；必须点击“用此项目开始”后才会采集，避免不同地点的数据混在一起
- 项目管理中可以新建、切换、修改名称和网络标注、删除项目；删除项目会同时删除该项目的历史记录
- 项目管理会列出每个项目已采集的本机时间段和记录数；两段数据相隔超过 5 分钟时分开显示
- 程序优先按当前 Wi-Fi 名称（SSID）推荐历史项目，再用公网运营商和地区辅助核对；手机热点和宿舍 Wi-Fi 名称不同，因此不会因同属联通而误推荐
- “网络环境横向对比”：选择开始和结束时间、对比指标（平均延迟 / P95 / 成功率）后，表格会并排展示不同测试项目（网络环境）在全部区域的对比结果，绿色高亮每一行的最优值；点击任意一行可查看该区域的柱状对比图，还可导出对比 CSV
- “节点管理”可以添加、批量导入、编辑、启用/停用或删除客户自己的服务器地址
- “暂停”停止后续检测但保留已有数据；“继续”从暂停处恢复
- “重新开始”会在确认后清空全部历史、轮次归零，并重新计算 48 小时监控时长

## 启动方式

- 双击 `OracleTCPMonitor.exe`：运行满 48 小时后停止继续采集，网页保持可查看到窗口关闭。
- 需要不限时运行时，可在命令行先设置 `TCP_DURATION_HOURS=0` 再启动 EXE。

请不要直接在 ZIP 压缩包预览窗口中运行；先把 EXE 解压到普通文件夹。请不要同时启动两次，否则会重复写入检测数据并争用网页端口。

## 修改参数

高级用户可通过环境变量修改：

- `TCP_INTERVAL=60`：检测间隔，单位秒，最低 5 秒
- `TCP_TIMEOUT=5`：单个地址连接超时，单位秒
- `TCP_DURATION_HOURS=48`：监控时长；设为 0 表示不限时

也可以直接在网页顶部填写并点击“保存设置”。网页设置会立即生效，并保存到 EXE 同目录的 `oracle_tcp_settings.json`，下次启动自动使用。

区域清单已嵌入 EXE，默认包含 Oracle 官方区域表的 45 个端点。

自定义节点支持域名、IPv4、IPv6 和 `1–65535` TCP 端口，最多 100 个。批量导入格式为“名称,地址:端口”，每行一个。甲骨文默认节点只能停用，不能删除；删除自定义节点会同时清除该节点的历史记录。请只检测自己拥有或获授权访问的服务器。

## 默认测试节点（45 个）

- 亚太：悉尼、墨尔本、海得拉巴、孟买、巴淡、大阪、东京、居銮、新加坡、新加坡西部、首尔、春川
- 欧洲：巴黎、马赛、法兰克福、米兰、都灵、阿姆斯特丹、约万诺瓦茨、马德里、马德里 3、斯德哥尔摩、苏黎世、伦敦、纽波特
- 北美：蒙特利尔、多伦多、克雷塔罗、蒙特雷、阿什本、芝加哥、凤凰城、圣何塞
- 南美：圣保罗、维涅杜、圣地亚哥、瓦尔帕莱索、波哥大
- 中东：耶路撒冷、利雅得、吉达、阿布扎比、迪拜
- 非洲：卡萨布兰卡、约翰内斯堡

完整节点代号及对应国家或地区请查看项目主页 README 的“默认测试节点”表格。

## 注意

- 测量的是到 Oracle Object Storage 服务入口的 TCP 建连时间，适合比较区域线路，但不等同于某台未来 VPS 的完整业务延迟。
- DNS 不通、端口不通或连接超时会记录为失败，不会用超时秒数伪装成延迟。
- SQLite 使用 WAL 模式，48 小时约产生 6.5 万条记录，普通电脑可轻松保存。
